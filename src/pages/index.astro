---
// "/" SERVE A VERSÃO LATEST.
// Quando a skill `landing-version` promove uma nova versão (latest muda em
// _config/versions.json), o IMPORT abaixo TEM que ser atualizado pra apontar
// pra o novo page.astro.
//
// Source of truth: _config/versions.json -> mpd.latest
// Diretório interno com lista de todas as versões: /dev (noindex via Base prop + X-Robots-Tag header no vercel.json)

import Page from '@versoes/mpd/v4/page.astro';
---

<Page />
